{
  "name": "population-service",
  "version": "1.0.0",
  "description": "Service exposing a REST-based API for rapid polling of city populations",
  "main": "src/index.js",
  "engines": {
    "node": "=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --silent ./test",
    "unit-test": "jest --silent ./test/unit",
    "integration-test": "jest --silent ./test/integration"
  },
  "author": "Steven Wallace",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "9.0.0",
    "express": "4.18.2",
    "lru-cache": "10.0.1"
  },
  "devDependencies": {
    "jest": "29.7.0"
  }
}
